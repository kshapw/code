# Local development (Ollama running on host)
LLM_BASE_URL=http://localhost:11434/v1
LLM_API_KEY=ollama
LLM_MODEL=sqlcoder

# Docker (set via docker-compose.yml environment, not here)
# LLM_BASE_URL=http://ollama:11434/v1
